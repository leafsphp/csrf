app:
  - src

actions:
  run:
    - lint
    - tests
  os:
    - macos-latest
    - ubuntu-latest
    - windows-latest
  php:
    extensions: json, zip
    versions:
      - '8.3'
      - '8.2'
      - '8.1'
      - '8.0'
      - '7.4'
  events:
    - push
    - pull_request
